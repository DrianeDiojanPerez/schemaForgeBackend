meta {
  name: GetSchema
  type: grpc
  seq: 2
}

grpc {
  url: {{grpc_url}}
  method: /schemaforge.v1.SchemaService/GetSchema
  body: grpc
  protoPath: ../../../proto/schemaforge/v1/schema.proto
  auth: inherit
  methodType: unary
}

body:grpc {
  name: message 1
  content: '''
    {
      "id": "{{schema_id}}"
    }
  '''
}
